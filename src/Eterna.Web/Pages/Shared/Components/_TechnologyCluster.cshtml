@model (string Title, string[] Items)
<div class="tech-cluster" data-tech-cluster>
    <p class="label">Cluster</p>
    <h3 class="tech-cluster__title">@Model.Title</h3>
    <ul>
        @foreach (var item in Model.Items)
        {
            <li>@item</li>
        }
    </ul>
</div>
